# .env.example

# Supabase / Prisma
# 用于后端服务和 Prisma ORM 连接到 Supabase 数据库的连接字符串
# 格式: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@[HOST]:[PORT]/postgres
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@[HOST]:[PORT]/postgres"

# Supabase Client (for frontend)
# 项目的公开 URL，用于前端 Supabase 客户端初始化
NEXT_PUBLIC_SUPABASE_URL="https://[YOUR_PROJECT_ID].supabase.co"

# Supabase 匿名密钥 - 用于前端公开访问
# 这个密钥是公开的，可以在前端代码中使用
NEXT_PUBLIC_SUPABASE_ANON_KEY="[YOUR_ANON_KEY]"

# Supabase 服务角色密钥 - 仅用于服务端
# ⚠️ 警告：此密钥拥有完全数据库访问权限，切勿在前端暴露
SUPABASE_SERVICE_ROLE_KEY="[YOUR_SERVICE_ROLE_KEY]"

# Next.js 认证配置
# 本地开发环境的 URL
NEXTAUTH_URL="http://localhost:3000"

# NextAuth.js 密钥 - 用于 JWT 签名和加密
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET="[GENERATE_A_RANDOM_SECRET]"
