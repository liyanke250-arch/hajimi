/**
 * Next.js 配置文件
 * 创建时间: 2025-08-24 15:35
 *
 * 开发思路:
 * 1. 使用 ES modules (.mjs) 格式，符合 Next.js 14 最佳实践
 * 2. 当前使用默认配置，保持简洁
 * 3. 后续可根据需要添加：
 *    - 图片优化配置
 *    - 环境变量配置
 *    - 重定向规则
 *    - 实验性功能开关
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // 当前使用默认配置
  // 未来扩展点：
  // - images: 图片优化配置
  // - env: 环境变量暴露
  // - redirects: 路由重定向
  // - experimental: 实验性功能
};

export default nextConfig;
